import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  loadingFrame: {
    width: '100%',
    height: '100%',
    display: 'grid',
    placeItems: 'center',
  },
}));
